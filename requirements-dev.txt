# Development / CI toolchain, exact-pinned so local runs match CI.
# Runtime deps live in each game's own requirements.txt (pygame-ce);
# Solitaire needs only the standard library.
ruff==0.15.1
mypy==1.19.1
bandit==1.9.4
pytest==9.0.3
pytest-cov==7.1.0
pip-audit==2.10.0
pre-commit==4.6.0
