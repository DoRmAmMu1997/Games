# Monopoly uses pygame-ce (the community-maintained fork). The code is
# compatible with the regular pygame import path, so either package works.
# The floor matches the other games in this repo.
pygame-ce>=2.5.2
